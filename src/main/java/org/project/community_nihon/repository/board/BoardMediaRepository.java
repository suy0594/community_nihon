package org.project.community_nihon.repository.board;

import org.project.community_nihon.domain.board.BoardMedia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardMediaRepository extends JpaRepository<BoardMedia, String> {
}
